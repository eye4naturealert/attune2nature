#### 8JUL2026

- W&OD is the only AOI with a reference layer, using mile markers in order to include proximity to them in the email alerts

- Using pathlib approach instead of hardcoding file paths, this way, file paths will not need to be
updated when changing to webserver, etc.
